# 更新日志

> publish 自动维护

- 2026-08-31 23:02 手动重跑验证（`997f5e1`）：`index 205行` + `01 161行` + `02 158行` + `03 152行` + `04 183行` 全量可读，核心摘要/灾害补入/链接自然化已校验，`RONI 1.0 / 周度+2.7 / >90% / 69%` 基线一致，待后续按需手动触发 `el_nino_update`。
